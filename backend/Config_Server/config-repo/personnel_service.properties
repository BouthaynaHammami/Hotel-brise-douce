# ===================================================================
# Configuration pour le microservice : personnel_service
# Centralisée dans le Config Server (Mode Native)
# ===================================================================

# --- Serveur ---
server.port=8086
server.servlet.context-path=/personnel/api
server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-stacktrace=always

# --- Base de données ---
spring.datasource.url=jdbc:mysql://localhost:3306/personnel?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Base_Securisee_123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA / Hibernate ---
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# --- Discovery (Eureka) ---
eureka.client.service-url.defaultZone=http://localhost:8761/eureka
eureka.instance.prefer-ip-address=false
eureka.instance.hostname=localhost
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# --- Cloudinary ---
cloudinary.cloud_name=ddtfi7kal
cloudinary.api_key=971417699379591
cloudinary.api_secret=SzcF1Ll17RR2oMvueIXikFAN2IM

# --- Logs & Debug ---
logging.level.feign=DEBUG
logging.level.org.springframework.cloud.openfeign=DEBUG

# --- Custom ---
my.custom.config=Configuration chargée depuis le serveur local !
